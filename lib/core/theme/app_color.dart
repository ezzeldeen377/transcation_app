import 'package:flutter/material.dart';

class AppColor {
  // Brand Colors
static const Color brandPrimary = Color(0xFF679BF1);
static const Color brandSecondary = Color(0xFF8AB9F1);
static const Color brandAccent = Color.fromARGB(255, 233, 207, 112);
static const Color brandHighlight = Color(0xFFFFC40A);
static const Color brandDark = Color(0xFF2F2E2C);
static const Color brandBlack = Color(0xFF050505);
static const Color white = Color(0xFFffffff);
static const Color errorColor = Color(0xFFD32F2F); // Red 700
static const Color errorLightColor = Color(0xFFFFEBEE); // Red 50
 static const Color darkGray = Color(0xFF1D1D1D);
 static const Color green = Colors.green;

// Main color for a branded feel
}
